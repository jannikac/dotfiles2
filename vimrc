" Configuration file for Vi Imporived, save as ~/.vimrc to use.

syntax enable " enable syntax processing

set encoding=utf-8 " encoding

set number " show line numbers
set clipboard=unnamedplus " enable clipboard for external programs

set incsearch " search as characters are entered
set hlsearch " highlicht matches
set ignorecase " search case insensitive
set smartcase " search case sensitive if caps is used
